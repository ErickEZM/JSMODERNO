// Seleccionar elementos con querySelectoAll
const card = document.querySelectorAll('.card'); // Retorna todos los elementos con ese selector
console.log(card);

const noExiste = document.querySelectorAll('no-existe');
console.log(noExiste);
