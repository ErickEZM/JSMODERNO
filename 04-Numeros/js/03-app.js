let resultado;

resultado = Math.PI;

// Redondear resultado
resultado = Math.round(2.8);
resultado = Math.round(2.4);
resultado = Math.round(2.5);

// Redondear hacia arriba
resultado = Math.ceil(2.5);
resultado = Math.ceil(2.1);

// Redondear hacia abajo
resultado = Math.floor(2.4);
resultado = Math.floor(2.9);

// Raiz Cuadrada
resultado = Math.sqrt(144);

// Valor absoluto. Ignora el negativo y se trae el numero
resultado = Math.abs(-20);

// Potencia
resultado = Math.pow(2, 4);

// Minimo
resultado = Math.min(1, 3);

// Maximo
resultado = Math.max(12, 5);

// Aleatorio
resultado = Math.random() * 20;

// Aleatorio dentro de un rango
resultado = Math.floor(Math.random() * 30);

console.log(resultado);