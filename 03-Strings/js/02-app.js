const producto = "Monitor 20 Pulgadas";

console.log(producto);

// Conocer la cantidad de letras de la cadena de texto
console.log(producto.length);

// Conocer si la palabra existe o no en el string (Si existe imprime la posicion, si no imprime -1)
console.log(producto.indexOf("Pulgadas"));

// Conocer si la palabra existe en el string (Si existe imprime true, si no false)
console.log(producto.includes("Monitor"));
